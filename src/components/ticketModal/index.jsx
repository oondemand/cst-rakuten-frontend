import { PrestadorGrid } from "./prestadorGrid";
import { TicketModalRoot } from "./root";
import { ServicesList } from "./servicesList";

export const TicketModal = {
  root: TicketModalRoot,
  prestadorForm: PrestadorGrid,
  servicesList: ServicesList,
};
